/*
  Make sure the exported variable or function names match
  the corresponding question
*/
function makeSum(a, b) {
  return a + b;
}

export const one = makeSum(69, 420);
export const two = "34asdf";
export const three = "312fd";
